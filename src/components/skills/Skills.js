import React from 'react';
import s from './Skills.module.css';

const Skills = (props) => {
    return (
        <div className={s.skills}>
            SKILLS
        </div>
    )
};

export default Skills;